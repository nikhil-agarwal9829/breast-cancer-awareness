# Server-side only — never put these in .js or .html files
GEMINI_API_KEY=
OPENAI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
OPENAI_MODEL=gpt-4.1-mini

# Supabase (Part 2+) — anon key is safe for browser; service_role is Edge Functions only (never in frontend)
SUPABASE_URL=
SUPABASE_ANON_KEY=
